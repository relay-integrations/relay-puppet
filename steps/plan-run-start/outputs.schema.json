{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The Relay API identifier for the plan run"
    },
    "jobID": {
      "type": "string",
      "description": "If using Puppet Enterprise, the orchestrator job ID for the plan run"
    }
  },
  "required": [
    "id"
  ]
}
